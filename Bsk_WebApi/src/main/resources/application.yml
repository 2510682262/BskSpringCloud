#spring.cloud.zookeeper.config.enable=true;
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 9091
spring:
  application:
    name: liapi
  cloud:
    loadbalancer:
      retry:
        enabled: true